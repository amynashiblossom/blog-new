---
title: 20260814_SQL 3일차_집계함수
tags:
  - GROUPBY
  - SQL
  - select
  - from절
  - where
  - groupby
  - havingwjf
  - orderby
date: 2026-08-14
publish: true
---
(복습)
limit는 몇 개까지만 나오게 할 것인가
limit 뒤에 offset은 
select *
from sample33
limit 3 offset3;
이면 3줄까지만 나오는데 offset3은 앞에서부터 3개는 넘기고 그다음부터(즉 4번째 행부터)
order by

함수 round() 반올림
concat(quantity,unit) from sample35;
는 quantity와 unit을 합쳐서 나오게 함

*메타문자 %는 어떠한 0개 이상의 문자*

----
집계 함수
주요 집계 함수
-count
-sum
-avg
-min
-max

count는 개수
![[20260814_SQL 3일차_집계함수-1.png]]
#distinct 는 중복된 값은 중복으로 1개로 보고 개수 반환

![[20260814_SQL 3일차_집계함수-2.png]]

Groupby랑 집계함수 같이 내면
각각의 그룹별로 집계함수 낼 수 있게 된다.

![[20260814_SQL 3일차_집계함수-4.png]]
round씌우고 ,2)면 소수점 2째자리까지 나오게 

#GROUPBY
#그룹바이 와 #집계함수 는 거의 함께 자주사용됨

그룹바이는 같은 값을 가진 행들을 하나의 그룹으로 묶어서 계산할 때 사용함.
그룹별 통계를 구할 수 있게 됨.

group by는 그룹별 통계를 내므로
(ex 국가별 통계) 많이 활용됨

group by select 절에는 
반드시 group bt에 들어간 칼럼 또는 집계 함수로 감싼 칼럼만 사용 가능

![[20260814_SQL 3일차_집계함수-5.png]]

그룹별로 평균낼 수 있다.
![[20260814_SQL 3일차_집계함수-6.png]]

![[20260814_SQL 3일차_집계함수-7.png]]

![[Pasted image 20260814150509.png]]

![[20260814_SQL 3일차_집계함수-9.png]]

![[20260814_SQL 3일차_집계함수-10.png]]

![[Pasted image 20260814152534.png]]

Syntax
select 그룹핑할컬럼,집계함수
from 테이블
where 조건
group by 그룹핑할컬럼
having 조건
order by 컬럼

[Group by+having]
SQL 실행순서가
FROM > WHERE>GROUPBY>HAVING>SELECT>ORDER BY여서
GROUP BY에는 having으로 조건건다

![[Pasted image 20260814155047.png]]


SELECT, Order by내에서 움직인다.
JOIN은 FROM절 내에서 작동

![[Pasted image 20260814155708.png]]


![[Pasted image 20260814155756.png]]
group by 주의해야 할 부분
having으로 묶자


연습문제 2번 주의
![[20260814_SQL 3일차_집계함수-16.png]]

!! group by를 사용한 쿼리에서는 정렬할때도 group by로 묶은 것 기준으로 정렬해야 함!!!!

![[20260814_SQL 3일차_집계함수-17.png]]

![[20260814_SQL 3일차_집계함수-18.png]]

집계할 때 꼭 group by로 묶었으면 group by로 묶은 함수로 정렬해야!!

연습문제 6번
정렬하려면 having 뒤에 order by 따로
 ![[20260814_SQL 3일차_집계함수-19.png]]

자꾸 실수내는 부분이 order by 뒤에 자꾸 조건을 걸려고 하는데 
true나 false를 갖는 조건절을 order by뒤에 둘 순 없음
order by는 어떠한 행을 기준으로 정렬해야하기에
![[20260814_SQL 3일차_집계함수-20.png]]

![[20260814_SQL 3일차_집계함수-21.png]]

연습문제2번의 어려움
![[20260814_SQL 3일차_집계함수-22.png]]

	연습문제 7번
	** 써주는 순서가
	select
	from
	where
	group by
	having
	order by
	실행순서는
	from - where- group by- having - select - order by

create 는 DDL 
insert , delete DML

오히려 desc:어떤 식으로 넣어야 하는지 보여주는 
이건 한 줄 띄어야 함
![[20260814_SQL 3일차_집계함수-23.png]]
![[20260814_SQL 3일차_집계함수-24.png]]
샘플1의 첫번째, 2번째, 세번째 키 모두에 집어넣은 사례

![[20260814_SQL 3일차_집계함수-25.png]]
![[20260814_SQL 3일차_집계함수-26.png]]


where no = 2 
![[20260814_SQL 3일차_집계함수-27.png]]
where 를 설정을 안하면 
set a = 'ooo' 의 a열의 모든 값이 다 ooo로 바뀌게 된다.

특정값 넣고싶으면 table이름 뒤로 칼럼 지정하면 됨
![[20260814_SQL 3일차_집계함수-28.png]]

!!delete는 정말 주의해야 함!!!

넣고자 하는 테이블(칼럼이름 지칭하면서)
insert into sample41(칼럼 첫번째, 칼럼 세번째)
values(첫번째에 넣을 값, 세번째에 넣을 값)
![[20260814_SQL 3일차_집계함수-29.png]]

되도록이면 delete는 조심! 지우지 않도록 조심

![[20260814_SQL 3일차_집계함수-30.png]]
customer 테이블의 모든 자료를 가져와서 복사하겠다

create, insert 연습문제 3번의 오답과 실제 답 비교해보자
![[Pasted image 20260814174600.png]]

바꾼 결과
![[20260814_SQL 3일차_집계함수-31.png]]

수정할때는 update 테이블
set 바꾸고자 하는 값; 
이렇게 하자!!
![[20260814_SQL 3일차_집계함수-32.png]]

